#pragma once

#include <vector>
#include <SDL.h>

class Scene {

public:
	Scene();
	~Scene();
	//void addObject();
	void draw(SDL_Renderer& renderer);
	
private:
	//std::vector<
	//Vector of objects.

};